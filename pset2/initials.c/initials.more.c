#include <ctype.h>
#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <stdlib.h>


int main(void){
    
string name = NULL;
do{
     printf("Enter your full name, please: ");
      name = get_string();
    //   if (isspace(name[0])){
    //     printf("Enter your full name, please: ");
    //     name = get_string();
    //       }else if(isspace(name[0])&&isspace(name[0+1])){
    //           printf("Enter your full name, please: ");
    //           name = get_string();
    if(b)
          } 
}
    
    while (name != NULL);
         bool b = true;
         int i = 0;
         //int len;
         for(i=1,len=strlen(name);i<len;i++){
    
             if (b == isalpha(name[i+0])) return true;
             else return false;
           
            printf("%c", toupper(name[i+0]));
            
           }
       
    

// while(name == NULL);
// if{
//  if (isalpha(name[0])){
//         printf("%c", toupper(name[0]));
//         }else{
//              printf("%c", name[0]); 
//              }    
// }else{
// bool flag false;
// int i, len;
// if ((isspace(name[i])&&isalpha(name[i+1]))==0)
// for(i=0,len=strlen(name);i<len;i++){
// {flag = true;break; printf("%c", toupper(name[i+1]));}//write flag
 

 }//for
 return 0;
 }
